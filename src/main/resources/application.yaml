spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:mobiledb
logging:
  level:
    org:
      hibernate:
        SQL:
          DEBUG
        type:
          descriptor:
            sql:
              BasicBinder:
                TRACE
server:
  error:
    include-stacktrace: never